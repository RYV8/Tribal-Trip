# Frontend environment variables.
#
# Same Vercel project as the backend: leave VITE_API_URL empty or omit it.
# Separate backend origin: set the full API base URL, including /api.
# Example: VITE_API_URL=https://api.example.com/api
VITE_API_URL=
